name: cashflow
description: A new Flutter project.

publish_to: "none"

version: 0.0.1+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.4
  english_words: ^4.0.0
  camera: ^0.11.1
  path_provider: ^2.1.5
  path: ^1.9.1
  image_picker: ^1.1.2
  intl: ^0.20.2
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Remove the assets block entirely if no other assets are needed